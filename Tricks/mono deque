// k is the length of the sliding window
deque<ll> q;
ll ans = 0;
    for (int i = 0; i < n; i++) {
        if (q.size() && q.front() == i - k) {
            q.pop_front();
        }
      // this get min
      while (q.size() && v[q.back()] >= v[i])q.pop_back();
          q.push_back(i);
      
    }
