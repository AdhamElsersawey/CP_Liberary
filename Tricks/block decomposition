//(gcd,and,or,min,max)
 vector<ll> pr(n), suf(n);
    for (int i = 0; i < n; i++) {
        if (i % k == 0)pr[i] = v[i];
        else pr[i] = pr[i - 1] | v[i];
    }
    for (int i = n - 1; ~i; --i) {
        if (i == n - 1 || i % k == k - 1)suf[i] = v[i];
        else suf[i] = suf[i + 1] | v[i];
    }
    for (int i = 0; i <= n - k; i++) {
        ans ^= suf[i] | pr[i + k - 1];
        بيحسب الحاجة في ويندو طولها k
    }
